# Domain
DOMAIN=voiture.delgehier.com

# Database Configuration
DB_ROOT_PASSWORD=CHANGE_ME
DB_NAME=historique_voiture
DB_USER=app_voiture
DB_PASSWORD=CHANGE_ME

# JWT Configuration
JWT_SECRET=CHANGE_ME
JWT_EXPIRATION=7d

# Ollama Configuration (for OCR)
OLLAMA_URL=http://172.17.0.1:11434
OLLAMA_MODEL=gemini-3-flash-preview:latest

# Web Configuration
WEB_PORT=80

# SMTP Configuration (pour l'envoi d'emails de reset password)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=CHANGE_ME
SMTP_PASS=CHANGE_ME
SMTP_FROM=noreply@voiture.delgehier.com
